# Epic 1 — Implementation Plan

**Stories:** 1.1, 1.2, 1.3
**Total Estimate:** 15 story points
**Generated:** 2026-07-15

---

## Project State Summary

| Component | Status | Action Required |
|---|---|---|
| Next.js scaffolding | DONE | Verify build |
| shadcn/ui init | DONE | Verify components.json |
| Dependencies installed | DONE | Verify package.json |
| Folder structure | DONE | Verify all dirs exist |
| Placeholder files | DONE | Verify exports |
| Middleware | DONE | Verify it works |
| Supabase clients | DONE | Need real credentials |
| globals.css | DEFAULT THEME | Replace with brand colors |
| Fonts | GEIST DEFAULT | Replace with Inter |
| shadcn components | ONLY BUTTON | Install 13 more |
| Supabase project | NOT CREATED | Create project |
| Database schema | NOT CREATED | Create migration |
| RLS policies | NOT CREATED | Apply policies |
| Storage buckets | NOT CREATED | Create buckets |
| Admin user | NOT CREATED | Create user |

---

## Phase 1: Story 1.1 — Verification & Cleanup

**Goal:** Confirm existing setup works, clean up boilerplate, ensure build succeeds.

### 1.1.1 — Verify Build
- [ ] Run `npm run build` and confirm zero errors
- [ ] Run `npm run lint` and confirm zero errors
- [ ] Fix any TypeScript errors if they exist

### 1.1.2 — Clean Up Boilerplate
- [ ] Update `src/app/layout.tsx` metadata:
  - Change title from "Create Next App" to "Boundless FC"
  - Change description from "Generated by create next app" to site description from `lib/constants/copy.ts`
- [ ] Clean up `src/app/page.tsx`:
  - Remove default Next.js boilerplate content
  - Either redirect to `(public)/page.tsx` or show minimal placeholder
- [ ] Verify `src/app/(public)/layout.tsx` exists and renders children
- [ ] Verify `src/app/(public)/page.tsx` shows "Boundless FC" placeholder

### 1.1.3 — Verify Folder Structure
- [ ] Confirm all directories exist:
  ```
  src/app/(public)/
  src/app/admin/
  src/app/api/
  src/components/blocks/
  src/components/admin/
  src/lib/supabase/
  src/lib/validations/
  src/lib/constants/
  src/lib/links/
  src/supabase/migrations/
  public/video/
  ```
- [ ] Create `src/hooks/` directory (referenced in components.json but missing)

### 1.1.4 — Verify Placeholder Files
- [ ] `src/lib/supabase/client.ts` — exports `createClient()`
- [ ] `src/lib/supabase/server.ts` — exports `createClient()` with cookies
- [ ] `src/lib/supabase/middleware.ts` — exports `updateSession()`
- [ ] `src/lib/constants/copy.ts` — exports `SITE_NAME`, `SITE_DESCRIPTION`
- [ ] `src/lib/constants/nav.ts` — exports `NAV_LINKS` array
- [ ] `src/lib/links/index.ts` — exports URL builder functions
- [ ] `src/lib/validations/session.ts` — exports `sessionRegistrationSchema`
- [ ] `src/lib/utils.ts` — exports `cn()` helper

### 1.1.5 — Verify Middleware
- [ ] `src/middleware.ts` exists and imports `updateSession`
- [ ] Matcher config excludes static assets
- [ ] Build passes with middleware in place

### 1.1.6 — Verify Vercel Connection
- [ ] Repo connected to Vercel (Hobby plan)
- [ ] Confirm deploy URL works (shows placeholder page)
- [ ] Environment variables set in Vercel dashboard (placeholder values OK for now)

**Exit Criteria:**
- `npm run build` succeeds with zero errors
- `npm run lint` succeeds with zero errors
- All placeholder files exist with correct exports
- Boilerplate cleaned up

---

## Phase 2: Story 1.2 — Design System & Components

**Goal:** Replace default shadcn theme with Boundless FC brand colors, load custom fonts, install all shadcn components.

### 2.2.1 — Replace Color Tokens in globals.css
- [ ] Replace `:root` CSS variables with brand colors:

```css
:root {
  --background: #151F2B;
  --foreground: #FAEAC2;
  --primary: #FF6C47;
  --primary-foreground: #FFFFFF;
  --secondary: #DB856A;
  --secondary-foreground: #151F2B;
  --muted: #1E2A38;
  --muted-foreground: #A0AEC0;
  --accent: #FAEAC2;
  --accent-foreground: #151F2B;
  --destructive: #FF4444;
  --destructive-foreground: #FFFFFF;
  --border: #2D3A4A;
  --input: #2D3A4A;
  --ring: #FF6C47;
  --card: #1E2A38;
  --card-foreground: #FAEAC2;
  --popover: #1E2A38;
  --popover-foreground: #FAEAC2;
  --radius: 0.625rem;
}
```

- [ ] Remove `.dark` section (site is dark-only per design)
- [ ] Remove sidebar variables (not needed for this project)
- [ ] Remove chart variables (not needed for this project)
- [ ] Verify `@theme inline` block maps all variables correctly

### 2.2.2 — Replace Fonts
- [ ] Update `src/app/layout.tsx` to import Inter:

```tsx
import { Inter } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});
```

- [ ] Update HTML className to use new font variable:
```tsx
<html lang="en" className={`${inter.variable} h-full antialiased`}>
```

- [ ] Update `globals.css` `@theme inline` to map fonts:
```css
@theme inline {
  --font-sans: var(--font-sans);
}
```

- [ ] Remove Geist font imports and variables

### 2.2.3 — Install shadcn Components
- [ ] Install all 13 remaining components:
```bash
npx shadcn@latest add card input textarea select label form dialog sheet carousel badge separator tabs sonner
```

- [ ] Verify all 14 components exist in `src/components/ui/`:
  - button (already exists)
  - card
  - input
  - textarea
  - select
  - label
  - form
  - dialog
  - sheet
  - carousel
  - badge
  - separator
  - tabs
  - sonner

### 2.2.4 — Verify Design System
- [ ] Create test component with `className="bg-primary text-primary-foreground"` — verify orange background, cream text
- [ ] Create test component with `className="text-foreground"` — verify gold/cream text
- [ ] Create test component with `className="border-border"` — verify dark border
- [ ] Create test component with `className="font-sans"` — verify Inter font
- [ ] Grep `src/components/` for raw hex codes — zero matches expected
- [ ] Run `npm run build` — zero errors expected

**Exit Criteria:**
- All brand colors applied in globals.css
- Inter font loaded via next/font
- All 14 shadcn components installed
- `npm run build` succeeds
- Zero hardcoded hex values in components

---

## Phase 3: Story 1.3 — Supabase Infrastructure

**Goal:** Create Supabase project, database schema, RLS policies, storage buckets, admin user.

### 3.3.1 — Create Supabase Project
- [ ] Go to https://supabase.com and create new project (Free Tier)
- [ ] Copy Project URL from Settings > API
- [ ] Copy Anon Key from Settings > API
- [ ] Copy Service Role Key from Settings > API
- [ ] Update `.env.local` with real values:
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-real-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-real-service-role-key
```

### 3.3.2 — Create Migration File
- [ ] Create `src/supabase/migrations/001_initial_schema.sql`
- [ ] Include all 8 tables with correct columns, types, constraints
- [ ] Include RLS enable statements
- [ ] Include public read policies
- [ ] Include public insert policy for session_registrations
- [ ] Include admin all-access policies

### 3.3.3 — Apply Migration
- [ ] Go to Supabase Dashboard > SQL Editor
- [ ] Paste migration content
- [ ] Execute migration
- [ ] Verify all 8 tables exist in Table Editor

### 3.3.4 — Create Storage Buckets
- [ ] Go to Supabase Dashboard > Storage
- [ ] Create bucket `tournament-posters` (private, 5MB limit, allow: image/*)
- [ ] Create bucket `product-images` (private, 5MB limit, allow: image/*)
- [ ] Create bucket `sponsor-logos` (private, 5MB limit, allow: image/*)

### 3.3.5 — Apply Storage Policies
- [ ] Go to Supabase Dashboard > SQL Editor
- [ ] Run storage policy SQL (authenticated upload + public read for each bucket)

### 3.3.6 — Create Admin User
- [ ] Go to Supabase Dashboard > Authentication > Users
- [ ] Click "Add user"
- [ ] Enter email and password
- [ ] Verify user is created and confirmed

### 3.3.7 — Verify Supabase Connection
- [ ] Run `npm run build` — zero errors
- [ ] Verify `createClient` imports work in server components
- [ ] Verify `createClient` imports work in client components
- [ ] Test basic query (optional — verify connection works)

### 3.3.8 — Update Vercel Environment Variables
- [ ] Go to Vercel Dashboard > Settings > Environment Variables
- [ ] Update `NEXT_PUBLIC_SUPABASE_URL` with real value
- [ ] Update `NEXT_PUBLIC_SUPABASE_ANON_KEY` with real value
- [ ] Update `SUPABASE_SERVICE_ROLE_KEY` with real value
- [ ] Trigger redeploy to apply new env vars

**Exit Criteria:**
- Supabase project created and connected
- All 8 tables exist with correct schema
- RLS policies applied and verified
- Storage buckets created with policies
- Admin user created in Auth
- `.env.local` has real credentials
- Vercel has real environment variables
- `npm run build` succeeds

---

## Final Verification

- [ ] `npm run build` succeeds with zero errors
- [ ] `npm run lint` succeeds with zero errors
- [ ] All brand colors applied (no default oklch theme)
- [ ] Inter font loaded
- [ ] All 14 shadcn components installed
- [ ] Supabase project connected with real credentials
- [ ] All 8 tables created
- [ ] RLS policies applied
- [ ] Storage buckets created
- [ ] Admin user created
- [ ] Vercel deploy works with real env vars
